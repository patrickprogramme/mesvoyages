{% extends "baseadmin.html.twig" %}

{% block body %}

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col">Ville</th>
                <th scope="col">Pays</th>
                <th scope="col">Date</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody class="table-group-divider">
            {% for visite in visites %}
                <tr>
                    <td>
                        {{ visite.ville }}
                    </td>
                    <td>{{ visite.pays }}
                    </td>
                    <td class="{{ visite.datecreationstring ? '' : 'text-muted' }}">
                        {{ visite.datecreationstring ?: '-' }}
                    </td>
                    <td>
                        <a href="#" class="btn btn-sm btn-outline-secondary">✏️</a>
                        <a href="{{ path('admin.voyage.suppr', {id: visite.id}) }}" 
                           class="btn btn-sm btn-outline-danger"
                           onclick="return confirm('Êtes-vous sûr de vouloir supprimer {{ visite.ville }} ?')">🗑
                        </a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}