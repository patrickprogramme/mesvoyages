{% extends "basefront.html.twig" %}

{% block body %}

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col">
                    <div class="d-flex align-items-center flex-wrap gap-2">
                        <span>Ville</span>
                        <a href="{{ path('voyages.sort', {champ:'ville', ordre: "ASC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▲</a>
                        <a href="{{ path('voyages.sort', {champ:'ville', ordre: "DESC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▼</a>
                        <form class="d-flex" method="POST" action="{{ path('voyages.findallequal', {champ:'ville'}) }}">
                            <input type="text" class="form-control form-control-sm me-2" name="recherche" placeholder="Rechercher une ville">
                            <input type="hidden" name="_token" value="{{ csrf_token('filtre_ville') }}">
                            <button type="submit" name="filtrer" class="btn btn-primary btn-sm"><i class="fas fa-search"></i></button>
                        </form>
                    </div>
                </th>
                <th scope="col">
                    <div class="d-flex align-items-center flex-wrap gap-2">
                        <span>Pays</span>
                        <a href="{{ path('voyages.sort', {champ:'pays', ordre: "ASC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▲</a>
                        <a href="{{ path('voyages.sort', {champ:'pays', ordre: "DESC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▼</a>
                        <form class="d-flex" method="POST" action="{{ path('voyages.findallequal', {champ:'pays'}) }}">
                            <input type="text" class="form-control form-control-sm me-2" name="recherche" placeholder="Rechercher un pays">
                            <input type="hidden" name="_token" value="{{ csrf_token('filtre_pays') }}">
                            <button type="submit" name="filtrer" class="btn btn-primary btn-sm"><i class="fas fa-search"></i></button>
                        </form>
                    </div>
                </th>
                <th scope="col">
                    Note
                    <a href="{{ path('voyages.sort', {champ:'note', ordre: "ASC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▲</a>
                    <a href="{{ path('voyages.sort', {champ:'note', ordre: "DESC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▼</a>
                </th>
                <th scope="col">
                    Date
                    <a href="{{ path('voyages.sort', {champ:'datecreation', ordre: "ASC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▲</a>
                    <a href="{{ path('voyages.sort', {champ:'datecreation', ordre: "DESC"}) }}" class="btn btn-sm btn-outline-primary" role="button">▼</a>
                </th>
            </tr>
        </thead>
        <tbody id="voyages-body" class="table-group-divider">
            {% for visite in visites %}
                <tr>
                    <td>
                        <a href="{{ path('voyages.showone', {id:visite.id}) }}">{{ visite.ville }}</a>
                    </td>
                    <td>{{ visite.pays }}</td>
                    <td class="{{ visite.noteColor }}">
                        {{ visite.notesurvingt }}
                    </td>
                    <td class="{{ visite.datecreationstring ? '' : 'text-muted' }}">
                        {{ visite.datecreationstring ?: '-' }}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

{% endblock %}