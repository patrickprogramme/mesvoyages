<!DOCTYPE html>
<html lang="fr">
  <head>
      <meta charset="UTF-8">
      <title>email</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f8f9fa;
        padding: 20px;
      }
      .card {
        border: 1px solid #dee2e6;
        border-radius: .5rem;
        background-color: #fff;
        padding: 20px;
        box-shadow: 0 2px 4px rgba(0,0,0,.1);
      }
      .card-title {
        font-size: 1.25rem;
        font-weight: 500;
        margin-bottom: 1rem;
        color: #0d6efd;
      }
      .card-text {
        margin-bottom: .75rem;
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h2 class="card-title">Nouveau message depuis MesVoyages</h2>
      <p class="card-text"><strong>Nom :</strong> {{ contact.nom }}</p>
      <p class="card-text"><strong>Email :</strong> {{ contact.email }}</p>
      <p class="card-text"><strong>Message :</strong><br>{{ contact.message|nl2br }}</p>
    </div>
  </body>
</html>